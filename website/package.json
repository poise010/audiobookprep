{
  "name": "audiobookprep-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs dist",
    "test": "node --test scripts/*.test.mjs",
    "check": "node --check assets/site.js && node --check assets/pricing.js && npm test && npm run build"
  }
}
